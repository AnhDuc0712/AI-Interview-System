# Operations

- Use `docker-compose up --build` to bring up frontend, backend, and vector services.
- Keep secrets in root or service-specific `.env` files.
- Run tests from the monorepo root using separate frontend/backend commands.
